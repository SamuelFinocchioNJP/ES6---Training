import { RandomcasePipe } from './randomcase.pipe';

describe('RandomcasePipe', () => {
  it('create an instance', () => {
    const pipe = new RandomcasePipe();
    expect(pipe).toBeTruthy();
  });
});
